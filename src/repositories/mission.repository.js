// ?? ??????? ??? ???? ??
export const addMissionToDatabase = async (missionData) => {
    // ??? ?????? ?? ??
    // ?: return db.collection('missions').insertOne(missionData);
    return { success: true, data: missionData }; // ?? ??
  };
  