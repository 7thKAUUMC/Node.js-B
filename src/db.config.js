import mysql from "mysql2/promise";
import dotenv from "dotenv";

dotenv.config();

export const pool = mysql.createPool({
  host: process.env.DB_HOST || "localhost", // mysql? hostname
  user: process.env.DB_USER || "root", // user ??
  port: process.env.DB_PORT || 3306, // ?? ??
  database: process.env.DB_NAME || "my_local_db", // ?????? ??
  password: process.env.DB_PASSWORD || "666666", // ????
  waitForConnections: true,
  // Pool? ??? ? ?? connection? ?? ?,
  // true? ??? queue? ?? connection? ??? ? ?? ?? ??? ????, false?? ?? ??? ???? ?? ??
  connectionLimit: 10, // ? ?? ???? ???? ? ???
  queueLimit: 0, // getConnection?? ??? ???? ?? Pool? ??? ??? ?? ??
});
