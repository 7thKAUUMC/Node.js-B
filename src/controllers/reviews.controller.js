import { StatusCodes } from "http-status-codes"; 
import { bodyToReview } from "../dtos/reviews.dto.js"; 
import { addReview } from "../services/review.service.js"; 

export const handleAddReview = async (req, res, next) => {
  console.log("?? ?? ??? ??????!"); // ?? ?? ??
  console.log("body:", req.body); // ?? ?? ??

  try {
    // ?? ??? DTO? ?? ??? ?, ?? ?? ??? ??
    const reviewData = bodyToReview(req.body);
    const review = await addReview(reviewData);
    
    res.status(StatusCodes.CREATED).json({ result: review }); // ?? ?? ? 201 Created ?? ??? ?? ?? ??? ??
  } catch (error) {
    console.error(error); // ?? ??
    res.status(StatusCodes.INTERNAL_SERVER_ERROR).json({ error: "?? ?? ? ??? ??????." }); // ?? ?? ? 500 ?? ??? ?? ??? ??
  }
};
